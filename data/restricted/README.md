# Restricted Data

This directory intentionally contains no row-level MIMIC-III derivative data.

Use `restricted_files_manifest.csv` to identify:

- which restricted CSV files exist or are expected;
- why they are excluded from GitHub;
- which public aggregate files replace them;
- how to regenerate them from an authorized MIMIC-III copy.

